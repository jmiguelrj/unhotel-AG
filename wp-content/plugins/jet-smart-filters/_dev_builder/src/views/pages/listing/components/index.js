import QuerySettings from "./query-settings";
import LayoutSettings from "./layout-settings";
import CardSettings from "./card-settings";

export { QuerySettings, LayoutSettings, CardSettings };

export default {
	settings: {
		Query: QuerySettings,
		Layout: LayoutSettings,
		Card: CardSettings,
	}
};